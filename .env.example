# Public-safe example only. Never place real credentials in this file.

# OpenAI-compatible provider (optional)
OPENAI_BASE_URL=https://your-openai-compatible-endpoint.example/v1
OPENAI_API_KEY=replace-with-local-secret
LLM_MODEL=replace-with-model-id
LLM_TIMEOUT_MS=25000

# Social intake (optional)
APIFY_TOKEN=replace-with-local-secret

# Shared store (optional)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=replace-with-local-secret

# Protected writes and scheduled routes
APP_ADMIN_TOKEN=replace-with-strong-local-secret
CRON_SECRET=replace-with-strong-local-secret
